'use strict';

var _isSelfAttached = require('./utils/isSelfAttached');

var _errorMessage = require('./utils/errorMessage');

// limit your circular reference
module.exports = function (source, limitation) {
  /*
  let limitObject;
  if(/) {
    for(let key in source) {
     limitObject = iterate(source, limitation)
  }
  }
  else {
    errorMessage(source)
  }
  return limitObject
  */
};